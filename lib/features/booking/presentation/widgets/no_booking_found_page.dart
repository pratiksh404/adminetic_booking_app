import 'package:flutter/material.dart';

class NoBookingFoundPage extends StatelessWidget {
  const NoBookingFoundPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No booking found"),
    );
  }
}
